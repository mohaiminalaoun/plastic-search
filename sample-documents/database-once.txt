A database stores durable information for an application. Each record follows an organized structure that supports reliable updates and queries. The application can request data by describing the values it needs. A cache may serve frequent reads faster, while the primary store remains responsible for correctness. Transactions protect related changes when several records must succeed or fail together under load.
