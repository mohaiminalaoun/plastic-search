A database stores durable information for an application. The database organizes records so services can create, read, update, and delete them consistently. Queries select data using fields and conditions. Indexes help the storage engine locate matching records without scanning everything. Transactions group related changes, while constraints reject invalid values and preserve correctness during concurrent requests from users under heavy traffic.
